# Archived task files

These are **frozen snapshots** of the local backlog as it stood on 2026-07-28, kept for
historical reference only. **Do not update them.**

Active work is now tracked in **[GitHub Issues](https://github.com/luucasorion/FaceClock/issues)**
(labels: `P2`/`P3` + domain). Decisions live in [`docs/adr/`](../adr/). Product requirements and
implementation status live in [`PROJECT_CONTEXT.md`](../../PROJECT_CONTEXT.md).

- `TASKS.md` — backend backlog (all done except the 8 items migrated to Issues #3–#13).
- `TASKS_FRONTEND.md` — frontend backlog (fully complete).
